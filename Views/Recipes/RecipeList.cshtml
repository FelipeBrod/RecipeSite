@model RecipesListViewModel

<p>
    <a asp-controller="CRUD" asp-action="AddRecipe" class="btn btn-success">Create New</a>
</p>

<table class="table table-striped table-hover">
    <tr class="success">

        <th>
            Name
        </th>
        <th>
           
        </th>
        <th>
            Cuisine
        </th>


    <tbody>

        @{ foreach (var item in Model.Recipes)
            {
<tr>
    <td>
        <h3><a asp-controller="Recipes" asp-action="ViewRecipe" asp-route-id="@item.Id" class="btn btn-link">@item.Name</a></h3>
    </td>
    <td>
        <h3><a asp-controller="Recipes" asp-action="ViewRecipe" asp-route-id="@item.Id" class="btn btn-success">Details</a></h3>
    </td>
    <td>
        <a asp-controller="Cuisine" asp-action="CuisineList" asp-route-id="@item.Cuisine.Id" class="btn btn-default">@item.Cuisine.Name</a>
    </td>
</tr> } }

    </tbody>


</table> @*<div page-model="@Model.PagingInfo" page-action="RecipeList"></div>*@